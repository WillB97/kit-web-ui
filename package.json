{
    "name": "kit-web-ui",
    "scripts": {
        "build:ui": "cd kit-ui && npm run build-django",
        "run:server": ". ./dev_env && django-admin runserver",
        "run:ui": "cd kit-ui && npm run watch-django",
        "start": "npm-run-all  --parallel run:*"
    },
    "devDependencies": {
      "npm-run-all": "^4.1.2"
    }
}